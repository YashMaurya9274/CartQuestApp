import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  starsContainer: {
    display: 'flex',
    flexDirection: 'row',
    gap: 2,
  },
  star: {
    height: 15,
    width: 15,
  },
});

export default styles;
